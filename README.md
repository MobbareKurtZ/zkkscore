# zkkscore

## Project Setup

Clone, cd into root and then
```sh
npm install
cd backend && npm install
```

### Build

(from root, not in backend/)
```sh
npm run build
```

### Run
```sh
cd backend && npm run start
```
